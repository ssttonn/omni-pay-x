apiVersion: v1
kind: Service
metadata:
  name: {{ include "routing-engine.fullname" . }}
  labels:
    app: {{ include "routing-engine.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "routing-engine.name" . }}
